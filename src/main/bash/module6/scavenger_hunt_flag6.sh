#/bin/bash
##Linux Scavenger hunt flag6
cd ~
#Run alias command to print the exising alias in the system
alias
#We then run the alias flag to get flag 6
flag

echo "For flag 7: Switch to sysadmin user use less as a sudo user and switch to a shell with in the less command"